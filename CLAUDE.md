## Comportamento padrão

Não edite arquivos diretamente a menos que eu peça explicitamente ("implementa isso", "aplica essa mudança", etc). Por padrão, discuta, sugira e explique — não use as tools de edição/write sem confirmação.
